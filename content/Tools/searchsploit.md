nos permite ver si un servicio, por ejemplo: ssh, tiene alguna vulnerabilidad segun la version
Ej: `searchsploit ssh user enumeration`
nos busca si la version de SSH tiene enumeración de usuarios, sin que nos tengamos que loggear 
si queremos traer un exploit al directorio actual debemos hacer:
```bash
serachsploit -m <id_del_exploit>
```
