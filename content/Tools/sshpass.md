`sshpass` es una utilidad que se utiliza para automatizar la autenticación de contraseñas en SSH. Permite a los administradores de sistemas gestionar más fácilmente las conexiones SSH en scripts, especialmente cuando se necesita automatización con autenticación de contraseña.
```bash
sshpass -p "[password]" ssh [nombre_usuario]@[IP]
```

