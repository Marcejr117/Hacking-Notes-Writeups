It is a tool that synchronizes our system clock with that of the DC, because if it is not synchronized it cannot obtain (for example), the TGT for a user.

# Use
```bash
sudo ntpdate -u 10.10.11.69
```
