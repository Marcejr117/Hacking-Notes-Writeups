esta herramienta nos mostrar los archivos abiertos por los procesos en ejecución
```bash
lsof -i
```

- Busca los archivos abiertos por los procesos que usan internet

```bash
lsof -i:80
```
- nos dice que el puerto 80 esta abierto